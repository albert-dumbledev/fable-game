# Fable FPS — First-Person Survivor-like

A first-person arena survival game. Monsters spawn continuously and run at you; you fight back with sword and shield. Difficulty ramps over time, punctuated by bosses. Dying is part of the loop: kills earn currency, and on death you spend it on permanent upgrades, then dive back in stronger.

---

## 1. Language: GDScript (chosen — platform flexibility first)

**Decision: GDScript**, prioritizing platform flexibility. GDScript exports everywhere Godot does — including web, which C# builds cannot target. Bonus: instant script reload (no build step) keeps combat-feel iteration fast, which matters a lot in Phase 1.

Discipline that makes GDScript hold up at full scope:

- **Static typing everywhere.** Type every variable, parameter, and return (`var speed: float`, `func take_damage(info: AttackInfo) -> void`). Enable the `untyped_declaration` warning as an error in project settings from day one. Typed GDScript catches most of what C#'s compiler would and is measurably faster at runtime.
- **`class_name` on every core type** (`AttackInfo`, `StatBlock`, `EnemyData`, …) so systems get real type checks and editor autocomplete instead of duck-typing.
- **Plan for the horde hot loop.** Script-side per-enemy logic (steering, attack timers) is GDScript's weak spot at scale. Mitigations, in escalation order: keep per-enemy `_physics_process` trivial → move steering into one manager iterating a packed array (data-oriented, see §3.6) → talk to `PhysicsServer3D` directly if node overhead ever dominates. The demo's 50-enemy target is comfortably fine; this matters at 200+.

Trade-off accepted: less compile-time safety and weaker refactoring tools than C#. The typing discipline above is the mitigation, not optional style. Stay single-language — no C# mixing.

---

## 2. Phase 1 — Playable Demo ✅ SHIPPED (and exceeded)

**Status:** done. The full loop — *spawn in → fight → die → buy upgrades → run again, measurably stronger* — is playable, and the demo overshot its scope: perfect-block parry (stuns the attacker), a three-phase sword swing animation, a rotating minimap, physical gold/XP pickups that burst from corpses and magnet to the player, and the full XP → level-up → boon-choice system with rarities all shipped ahead of schedule. See §5 for the per-phase record.

**Original goal (kept for the record):** one full loop with graybox visuals. Everything below was deliberately minimal but built on the real architecture from §3 so nothing got thrown away — which is exactly what happened.

### Demo scope

| Piece | Demo version |
|---|---|
| Arena | Flat graybox floor with boundary walls, one `WorldEnvironment` for basic lighting |
| Player | FPS controller (WASD + mouse look, sprint optional), capsule body |
| Sword | LMB swing: short cooldown, hits via an `Area3D` hitbox arc in front of camera; simple viewport-space swing animation (even just a rotating mesh) |
| Shield | Hold RMB to block: negates (or heavily reduces) damage from the front ~120°, drops move speed while held |
| Enemy | One melee chaser: capsule with eyes, straight-line steering toward player, windup → lunge attack → recover. No navmesh yet — open arena doesn't need it |
| Spawning | Timed spawner placing enemies in a ring outside player view; spawn interval shrinks and enemy HP/damage scale with elapsed run time |
| Damage/health | Shared health component on player and enemies; hit flash + death poof placeholder |
| Currency | Gold per kill, floats up in HUD |
| Death → upgrade | Death screen shows run stats + upgrade shop: **+Damage, +Max Health, +Attack Speed, +Move Speed** (escalating costs, no caps). Buy, then "Next Run" |
| Persistence | Gold + purchased upgrade levels saved to `user://save.json` so quitting doesn't wipe progress |
| HUD | Health bar, gold counter, run timer, kill counter |

### Demo acceptance criteria

1. A run lasts 2–5 minutes for a fresh character and visibly gets harder.
2. Dying never feels like a dead end — you always afford *something* after a run.
3. Three runs in, the power increase is obviously felt (kill speed, survivability).
4. 60 fps with 50+ live enemies on a mid-range machine.

### Explicitly *not* in the demo (then)

Bosses, XP/level-ups, second weapon, spells, sound design, real models/animations, menus beyond death screen, prestige. Of these, **XP/level-ups shipped in Phase 2** and **bosses, the second weapon, and two spells shipped in Phase 3**; the rest remain Phase 4+.

---

## 3. Core Architecture (extensibility design)

Guiding rule: **behavior lives in scripts/scenes, numbers and content live in `Resource` files.** Adding an enemy, weapon, or upgrade should mean creating a `.tres` file and (at most) one scene — no edits to core systems.

### 3.1 Autoload singletons

| Autoload | Responsibility |
|---|---|
| `EventBus` | Global signals only, no logic. The list has grown past the original five — it now covers the run lifecycle (`run_started`, `run_ended`), combat (`enemy_killed`, `player_damaged`, `player_died`, `attack_blocked`, `perfect_block`), economy (`currency_changed`, `pickup_collected(kind, value)`), and progression (`xp_changed`, `level_up`). See `autoload/event_bus.gd` for the canonical list; the rule stands: signals only, no state |
| `GameManager` | Top-level state machine: `Menu → InRun → DeathScreen → (shop) → InRun`. Owns scene transitions |
| `MetaProgression` | Persistent state: currency balances (a `Dictionary[StringName, int]` keyed by currency id — this is the prestige hook), purchased upgrade levels, save/load to `user://`. Exposes `get_stat_modifiers()` consumed by the player on spawn |
| `AudioManager` *(Phase 4)* | Owns the SFX bus and two player pools (flat + positional). All sounds are **procedurally synthesized at startup** by `SfxFactory` (`core/sfx_factory.gd`) — no audio assets, web export stays asset-free. Global cues (kills, blocks, pickups, level-ups, bosses) attach via EventBus; actor-local moments (swings, casts, impacts) call `play()`/`play_at()` directly. Per-id rate limiting stops coin fountains stacking 16 blips into one frame |
| `Settings` *(Phase 4)* | User options persisted to `user://settings.json` (separate from the save): mouse sensitivity, FOV, screen shake, master/SFX volume, fullscreen. `apply()` pushes to the engine and emits `changed`; the player re-reads live. Declared after AudioManager so the SFX bus exists before volumes apply |

### 3.2 Component scenes (composition over inheritance)

Small reusable nodes attached to any actor:

- **`HealthComponent`** — `Max`, `Current`, `TakeDamage(AttackInfo)`, signals `Damaged`, `Died`. Identical on player and monsters.
- **`HitboxComponent`** (Area3D) — carries an `AttackInfo` when active; **`HurtboxComponent`** (Area3D) — receives it, routes through the owner's mitigation (block check, armor later) into `HealthComponent`.
- **`AttackInfo`** (lightweight `RefCounted` class with `class_name`) — currently `source` + `damage` (damage types/knockback are still future fields). Every damage event flows through this one pipeline — melee, enemy punches, and spitter projectiles alike — which is how blocking, thorns, and damage numbers all hooked in without touching callers. Full pipeline documented in **[docs/COMBAT.md](docs/COMBAT.md)**.

### 3.3 Stats system (the most important extensibility piece)

- **`StatBlock`** — dictionary of stat id → base value plus a modifier list, with per-stat caching. Canonical ids live in `core/stats.gd` — the four base stats (`max_health`, `damage`, `attack_speed`, `move_speed`) plus base-1.0 multiplier stats added as systems grew: `spell_cooldown`, `cast_time`, `fireball_aoe`, `fireball_charges`, `hammer_aoe`, and (Phase 5) `riposte_damage`, `parry_stun`, `hammer_shove`, `spell_damage`.
- **`StatModifier`** — `{ stat, Flat | PercentAdd | PercentMult, value }` as a `Resource`, so `.tres` files can embed them. Resolution order: base → sum flats → ×(1 + sum additive %) → ×∏(1 + multiplicative %).
- Upgrades, in-run boons, weapon bonuses, and future prestige multipliers are *all just modifier sources*. Nothing else in the game hardcodes a stat formula. This bet paid off: the entire boon rarity system is "duplicate the modifier, multiply its value" (see [docs/BOONS.md](docs/BOONS.md)).

### 3.4 Data resources (content as `.tres` files)

- **`EnemyData`** — display name, scene, base stats, attack timing (`windup_time`, `recover_time`, `attack_range`), gold/XP reward, spawn weight, `min_elapsed` time gate, tags (`boss`, …). Four enemy types exist as pure data files. Full reference: **[docs/ENEMIES.md](docs/ENEMIES.md)**.
- **`WeaponData` / `WeaponRegistry`** — id, damage, swing time, `can_block`, scene path, `unlock_ability`, and (Phase 5) `grants_abilities` (flags granted while the weapon is mounted — the staff grants `firebolt`). Weapon behavior = a `Weapon` base class (`try_attack()`, `try_secondary()`, `set_blocking()`, `set_stowed()`, `notify_block_success()`); **three** subclasses: Sword & Shield, Warhammer, and the Battle Staff. **Loadout:** one weapon per run, picked on the death screen from `data/weapons/registry.tres`; weapon unlocks now come from **boss drops** (Phase 5), not the gold shop; the choice persists in the save (`MetaProgression.selected_weapon`) and the player instances the scene into its weapon mount on spawn. Details: [docs/COMBAT.md](docs/COMBAT.md).
- **`UpgradeData`** — id, name, description, base cost + geometric cost growth, `StatModifier[]` granted per level, max level (0 = infinite), tree placement `branch` (&"might" / &"vigor" / &"arcana") + optional `requires_upgrade` + `requires_level` gate, plus (Phase 5) `requires_ability` (hides the card entirely until a flag is owned — weapon subtrees appear only after their boss drop) and `loadout` (a weapon id — the upgrade only applies and only shows while that loadout is selected, so each loadout has its own tree). 100% data-driven from `data/upgrades/registry.tres`.
- **`BoonData` / `BoonRegistry`** *(new since the original plan)* — run-scoped level-up rewards. Same `StatModifier` machinery as upgrades, only the lifetime differs; unique boons grant ability flags instead. Gating via `requires_weapon` / `requires_any_ability`; optional `rarity_mults` overrides the shared rarity scaling for a bespoke curve. Full reference: **[docs/BOONS.md](docs/BOONS.md)**.
- **`WaveTable` / `WaveEvent`** — spawn interval ramp, HP/damage/reward growth per minute, alive-cap ramp, weighted + time-gated enemy pool, and scheduled events with `repeat_every` (bosses + repeating swarms — shipped). The spawner reads this; new content = new rows.

### 3.5 Run Director (per-run scene node)

Owns the in-run loop: elapsed time, spawner pacing, XP/level bookkeeping (level-ups fire the boon choice), and the death → stats handoff to `GameManager`. Kill and gold tallies count *collected pickups*, not kills — rewards are physical drops now (§4). The handoff dict (`time`, `kills`, `gold`, `level`) now also carries a full **run recap** built by `RunStats` (`core/run_stats.gd`) — a child node accumulating kills-by-enemy, attributed damage taken, killer, boons/Aspects picked, and boss kill times off EventBus — plus `new_records` from `MetaProgression.record_run()` (lifetime bests). Design: **[docs/RUN_RECAP.md](docs/RUN_RECAP.md)**.

### 3.6 Enemy AI

Shipped as designed, plus one state: `Chase → Windup → Attack → Recover`, with **`Stunned`** added for the perfect-block reward. States are methods on `EnemyBase` — the ranged Spitter overrides `_chase()` (kiting) and `_begin_attack()` (projectile) and inherits everything else, which validated the "override behavior, not plumbing" bet. Attacks are telegraphed (windup color shift + fist cock-back, then a punch lunge). Details and authoring guide: **[docs/ENEMIES.md](docs/ENEMIES.md)**. NavigationAgent3D still deferred until arenas gain obstacles; the data-oriented steering escape hatch remains unused and available.

### 3.7 Scene/folder layout

```
res://
  autoload/        event_bus.gd, game_manager.gd, meta_progression.gd,
				   audio_manager.gd, settings.gd
  components/      HealthComponent, Hitbox/HurtboxComponent
  core/            StatBlock, StatModifier, Stats (id registry), AttackInfo,
				   EnemyData, WeaponData, UpgradeData(+Registry),
				   BoonData(+Registry), WaveTable, DamageNumber, SfxFactory,
				   GroundTelegraph
  data/
	enemies/       *.tres (EnemyData) — chaser, sprinter, brute, spitter,
				   juggernaut, caster
	weapons/       sword.tres, warhammer.tres, staff.tres + registry.tres (loadout pool)
	upgrades/      *.tres + registry.tres — per-loadout trees (sword/hammer/staff)
	boons/         *.tres + registry.tres (drives the level-up screen)
	waves/         default.tres (WaveTable)
  actors/
	player/        Player.tscn + controller, camera rig, weapon mount
	enemies/       enemy_base.gd, boss_base.gd, boss_enemy.gd (Juggernaut),
				   caster_boss.gd (Hierophant), boulder_projectile.gd,
				   enemy_fireball.gd, ChaserEnemy/Sprinter/Brute/Spitter/
				   BossEnemy/CasterBoss.tscn, Projectile.tscn
	pickups/       Pickup.tscn (gold/XP/magnet/health/unlock-relic kinds)
  weapons/         weapon.gd, SwordAndShield/Warhammer/Staff.tscn, Fireball.tscn,
				   ArcaneBolt.tscn, ground_shockwave.gd
  systems/         run_director.gd, spawner.gd
  ui/              HUD.tscn (incl. minimap + boss bars), DeathScreen.tscn,
				   BoonScreen.tscn, ClaimScreen.tscn, MainMenu.tscn (boot scene),
				   PauseMenu.tscn, SettingsPanel.tscn
  levels/          Arena.tscn
  docs/            BOONS.md, COMBAT.md, ENEMIES.md, PROGRESSION_REWORK.md
```

---

## 4. Progression & Balance Dimensions

**Shipped: the dual-track economy, with a twist the original plan didn't have — rewards are physical objects.**

- **Gold** (persistent): spent on permanent meta upgrades at the death screen (`data/upgrades/`, geometric cost growth ~×1.4/level), and doubles as an *in-run* sink (boon rerolls) and *in-run* source (boon skips).
- **XP** (per-run): fills the HUD bar; threshold is a gentle geometric curve (`14 × 1.14^level`, Phase 5) targeting a level every ~10–30s and letting swarm/boss XP chunks pop multiple levels at once. Each level-up pauses the run and offers 3 boons with rolled rarities (Common ×1.0 → Legendary ×3.5, or a boon's own `rarity_mults`) — the Vampire Survivors moment, working as intended. Full design: **[docs/BOONS.md](docs/BOONS.md)**.
- **Both drop as pickups, not HUD increments.** Enemies explode into up to 8 gold + 8 XP pieces that burst ballistically, bounce, then magnet to the player. Rewards are granted on *collection*, not on kill — leaving loot on the ground when overwhelmed is a real cost, and diving into a pack to vacuum a fountain is a real risk. Drop values scale with run time (`WaveTable.reward_growth_per_min`, +30%/min) so late-run kills stay worth collecting.

Balance levers this creates: risky play rewards a stronger *current* run (XP boons, loot diving), safe farming maximizes *permanent* growth (gold), and the skip-vs-take boon decision converts unwanted level-ups into meta progress. **Loadout identity (Phase 5):** the three weapons now play distinct games — sword = parry→riposte Duelist, hammer = pull-and-slam Earthshaker CC, staff = spell-slinging Arcanist — each with its own boon family (`requires_weapon`) *and* its own meta-upgrade tree (`UpgradeData.loadout`), so switching weapon means rebuilding base power. Everything is still `StatModifier`s on a shared `StatBlock`; the unique-boon ability flags are the non-stat build differentiators.

**Prestige (design for, don't build):** the hooks already exist — currencies are keyed by id in `MetaProgression`, so prestige is: add a `prestige_shards` currency earned from a reset condition, a "reset gold + upgrades" operation, and a prestige upgrade folder whose modifiers apply globally. No system rewrites required.

---

## 5. Phased Roadmap

| Phase | Status | What shipped / what's left |
|---|---|---|
| **0. Setup** | ✅ Done | Folder skeleton, autoloads, input map (incl. a later `dash` action), `untyped_declaration` as error. |
| **1. Demo** | ✅ Done | Full loop per §2 — FPS controller, sword/shield with three-phase swing, chaser enemy with telegraphed attacks, ring spawner, gold economy, death-screen shop, JSON save. Plus unplanned extras: perfect-block parry, block feedback pass. |
| **2. Depth** | ✅ Done (SFX deferred) | XP + boon choices with rarities and unique ability boons; three new enemy types (Sprinter/Brute/Spitter) as pure `EnemyData`; hit feedback (damage numbers, trauma camera shake, hit-pop, damage vignette); rotating minimap; physical gold/XP pickups. **SFX was cut from this phase — no audio assets exist yet; it moves to the Phase 4 audio pass.** |
| **3. Bosses & arsenal** | ✅ Done | **Juggernaut boss** via scheduled `WaveTable` events (telegraphed wall-to-wall charge that phases through the player and plows minions aside; parry cancels it) with boss HP bar + spawn banner; **hit knockback** on every enemy attack (`AttackInfo.knockback`, per-enemy strength); **Fireball** — 0.8s committed charge (sword/shield stow, both hands busy) → AoE explosion — unlocked via ability-granting `UpgradeData`; **dash reworked** into a fixed-distance intangible blink; skill cooldown HUD; **weapon loadout** — one weapon per run, picked on the death screen, unlocked via the same ability-upgrade path; **Warhammer** — slow two-handed slam with a ground-AoE shockwave and shove, `can_block = false` (the shield is the price); **Frost Nova** — instant defensive AoE (E) that chills enemy *movement* to ×0.35 for 3.5s, never attack telegraphs. |
| **3.5 Depth & chaos** *(unplanned)* | ✅ Done | **Controls:** dash moved to Shift (sprint removed — dash is the mobility tool), jump on Space. **Build-specific boons:** `BoonData` gained `requires_weapon`/`requires_any_ability` gating; new stats (`spell_cooldown`, `cast_time`, `fireball_aoe`, `fireball_charges`, `hammer_aoe`); 10 new boons — sword (thorns now gated here + Duelist's Focus parry window), hammer (Wide Tremor, Aftershock), spells (Quick Mind, Fast Hands, Greater Blast, Twin Flame, Scorched Earth burning ground, Echo Nova, Glacial Wave). **Lethality:** base HP 100→80, Vitality upgrade halved to +10, enemy damage up ~1.5–1.8×, damage growth +25%→+40%/min — a fresh player dies in ~4 hits; Bulwark picks are the counterplay. **Chaos:** sprinters 8.5 speed, spitters fire ~2× as often, all telegraphs ~25% snappier, spawn interval 1.8→0.25s, alive cap 24→90, and repeating **SWARM events** (`WaveEvent.repeat_every`): 12 sprinters every 75s, 16 chasers every 150s. **Follow-ups:** Scorched Earth reworked to burn the blast point (zone denial, not a trail); blocking became a **guard meter** (3s hold, hits cost extra, perfect blocks free, break = full-refill lockout); warhammer gained **Seismic Slam** (RMB): long overhead windup → forward line shockwave via `Weapon.try_secondary`. |
| **4. Meta & polish** | ◐ Core shipped (2026-07-03) | **Shipped:** main menu (now the boot scene; Quit hidden on web) + pause menu on Esc (resume / settings / abandon run — abandon banks gold and routes to the normal death-screen handoff) + shared settings panel; `Settings` autoload (sensitivity, FOV, shake, volumes, fullscreen → `user://settings.json`); **full SFX pass with zero assets** — ~21 sounds synthesized at startup (`SfxFactory`: tone sweeps + filtered noise + envelopes), wired via EventBus + direct calls (swings per-weapon, slams, parry vs block, guard break, coin/XP blips, level-up stinger, boss horn, swarm alarm, UI clicks); **upgrade tree UI** — shop rebuilt as three prerequisite-gated branches (Might: Damage → Ferocity → Warhammer; Vigor: Vitality → Swiftness; Arcana: Fireball → Frost Nova). **Left:** balance pass on curves (blocked on playtest of this build), art pass, music. |
| **5. Progression rework** | ✅ Done | Full design: **[docs/PROGRESSION_REWORK.md](docs/PROGRESSION_REWORK.md)**. Milestones: **M1 — rare utility drops ✅** (magnet pickup vacuums all loot on collect, rare one-at-a-time drop with minimap ping; health pickup heals 25% max, per-kill roll + guaranteed on boss kills; two new synth SFX). **M2 — boss-drop unlocks ✅** (weapon unlocks left the gold shop; `EnemyData.unlock_drops` + a `&"unlock"` relic pickup — juggernaut drops the first unowned weapon as a glowing relic you walk over to claim, `MetaProgression.grant_meta_ability` persists it immediately, equip next run; warhammer removed from the shop; save migration is legacy-key-driven & idempotent — veterans keep the hammer + get refunded 250g, verified). **M3a — shared plumbing + riposte core ✅** (`UpgradeData.requires_ability` hides shop cards entirely until the flag is owned; 4 new base-1.0 mult stats riposte_damage/parry_stun/hammer_shove/spell_damage; **Riposte** — a perfect block primes a 2s window, the next sword swing deals +75% to every enemy it hits then consumes the prime, blade glows gold; parry stun now scales with parry_stun; Counterweight Vigor upgrade +10% riposte/lvl behind Vitality). **M3b — sword boon family ✅** (6 `requires_weapon` sword boons: Ruthless Riposte +40% riposte dmg & Punishing Stun +30% stun (stackable stats); Retribution `parry_nova` 3m pulse; Expose Weakness `exposing_parry` +35% dmg-taken while stunned via `EnemyBase.mitigate_hit`; Second Wind `parry_heal` heal+guard; Blade Cyclone `riposte_sweep` full-circle riposte). **M4 — hammer Earthshaker ✅** (shove force now scales with hammer_shove for slam + Seismic wave; Wrecking Ball +25% shove stat; Implosion `slam_pull` gathers the pack inward + 0.5s stagger; Riptide `wave_drag` drags the wave's catch into a staggered clump at the end; Bone Breaker `shove_impact` — enemies slammed into walls take 30% dmg + stagger via `EnemyBase._wall_impact`; all gather-stuns guarded against re-stun locks; Heavyweight Might upgrade +10% shove/lvl gated behind the warhammer drop). **M5 — staff Arcanist ✅** (new `battle_staff` loadout: LMB Arcane Bolt + RMB Fireball via `try_secondary` (staff no longer stows mid-charge); spells are staff-only (`weapon is Staff` gates frost nova + HUD slots); `WeaponData.grants_abilities` grants firebolt from the mount; firebolt left the shop, frost nova returns as the staff-gated Arcana root + Attunement/Overcharge (spell_cooldown/spell_damage); juggernaut drops the staff after the hammer; save migration extended: firebolt→weapon_staff+refund, frost_nova→weapon_staff+keep). **Post-launch balance/UX pass ✅** (two rounds of playtest feedback): drop feel (collectable sooner + bigger zone, magnet overshoot fixed so it reliably *collects*, no health pickup at full HP); sword (Second Wind → 25% next-swing lifesteal, new Reflex Guard 360°-block boon); warhammer (nerf 26→20 dmg, 210° frontal arc with a blind wedge behind you, Implosion moved to the Seismic Slam as a snappy gather-to-centre, only the inner core pushes, wall-slam damage no longer stuns); XP pacing (geometric curve ~10–30s/level, multi-pops on swarms/bosses) + gentler/rhythmic difficulty cadence; **per-loadout upgrade trees** (`UpgradeData.loadout`; each loadout has its own Damage/Vitality/Speed cards + weapon specifics, filtered at runtime — sword reuses the old ids so veteran saves carry over with NO migration, hammer/staff build fresh; distinct DUELIST/EARTHSHAKER/ARCANIST shop theming); **boss-loot UX** (relic drops only after ALL wave bosses die, arena clears + spawns pause, walk to relic → paused claim-screen modal → resume, two boss health bars); spell boons gated staff-only; Wide Tremor AoE given a gentle bespoke rarity curve (`BoonData.rarity_mults`). |
| **6. Boss rework** | ✅ Done (playtest tuning pending) | Full design: **[docs/BOSS_REWORK.md](docs/BOSS_REWORK.md)**. **Shared tech (M1):** `GroundTelegraph` (display-only ground danger decal that fills over a windup) + `Player.apply_shove` (no-damage push, dash still escapes); `BossBase` (M3a) extracted from the Juggernaut for the shared death spectacle + loot wave, so both bosses inherit it. **Juggernaut kit (M2):** the melee punch became a warhammer with two distance-picked attacks — a facing-locked telegraphed **hammer slam** (ground AoE routed through the player hurtbox) up close, and a lobbed **boulder mortar** (`BoulderProjectile`: ballistic arc to a committed ground zone, no mid-flight collision, dashing through the arc is safe) at range; the signature charge is unchanged, and the three modes are interleave-gated so none fires inside another's window. **Caster boss — THE HIEROPHANT (M3–M4):** the minute-5 wave is now one slow kiting spellcaster (`caster_boss.gd`) that backs away + wall-slides and casts on a priority rotation — Arcane Bolt / Triple Fireball (`EnemyFireball` ±14° fan) / Arcane Eruption (three chained rifts tracking the player) — plus a **repulse**: the first hit taken arms a 1.5 s fuse that flings the player clear for 0 damage (un-blockable, dash-escapable, and stun-pausable so a remote parry-stun stretches the burst window) — the anti-melee pacing valve. New procedural SFX (boulder thud, repulse whoomp, eruption crack). **Schedule + victory (M5):** the staff drop moved off the Juggernaut onto the Caster, and collecting the staff ended the run in victory *(superseded by the Phase 7 rescale — the staff is now a normal weapon unlock and victory moved to a 7:30 finale boss, THE REVENANT)*; veterans already owning the staff get no drop and play on endless. Each milestone was runtime-smoke-tested headless. |
| **7. Enemy expansion** | ✅ Done (playtest tuning pending) | Full design: **[docs/ENEMY_EXPANSION.md](docs/ENEMY_EXPANSION.md)**. Generic `EnemyData.death_spawns` + `death_spawn_count` plumbing for spawner mobs; **Broodmother** (slow carrier death-bursting into 5 Broodlings — kill position is a decision, feeds Earthshaker AoE). **Stalker** (arc-engage, timed-disengage skirmisher chipping the guard meter; parry→riposte is the counter). `WaveEvent.chance` field (random event rolls); **Gilded One** (rare chance-event fleeing bounty with gold minimap blip, steers toward enemy clusters). `Pickup` edible registry + `consume()` plumbing; **Scavenger** (loot-triggered non-attacking rat eating uncollected gold/XP, burrows with bounty ×1.25 — the economy's missing predator). M5 balance: pool spawn weights retuned (Sprinter 0.8→0.67, Brute 0.35→0.4, Broodmother 0.27). All milestones headless-verified. **7:30 rescale (post-M5):** the run now targets **7:30** in preparation for a 3rd boss — bosses stay at 2:30/5:00, the alive cap climbs to 120 by 6:30 (was 90 by 4:30), the staff became a normal unlock, THE REVENANT finale boss spawns at 7:30 and its death ends the run in victory (see Phase 8D), and all four expansion enemies moved into the new 5:00–7:30 third act (Stalker/Scavenger 300s, Broodmother 330s, Gilded 315s, "THE BROOD COMES" 390s). |
| **8. Fun & challenge rework** | ✅ Done (playtest tuning pending) | Full designs: **[8A — Weapon balance](docs/WEAPON_BALANCE_REWORK.md)**, **[8B — Scaling & threat](docs/SCALING_REWORK.md)**, **[8C — Loadout mobility](docs/MOBILITY_REWORK.md)**. **8B (M1–M5):** boon power compressed (mid-rarities, time-scaled rarity scaling with run time), geometric enemy-HP curve, boss HP ~2–3× raise, Broodling swarm burst, Stalker pounce threat bump. **8A (M1–M6):** hammer outer ring shove-only (control identity over splash damage, core size 2.0 → 1.6), Seismic Slam snappier with wall-to-wall reach, staff mana economy (Arcane Bolt generation → Fireball/Frost Nova spends), staff primary-fire boon family (Split Shot, Scatter Shot, Burst Fire, Arcane Surge), all verified headless. **8C (M1–M5):** Shift becomes per-loadout mobility — duelist keeps blink + gains charges + Shield Dash riposte, Earthshaker **Crashing Leap** (arc jump with full-damage slam circle), Arcanist **Levitate** (~2.5s hover with air-strafe and full casting, drains ~8 mana/s while airborne). Each milestone was runtime-verified. |
| **8D. Finale boss — The Revenant** | ✅ Done (playtest tuning pending) | The 7:30 run-ender. **Stalker-type orbiter** that engages on a cooldown rhythm: blinks in along a telegraphed lane, executes a **Cadence Combo** (3-hit Slash → Backhand → Overhead finisher), opens a vulnerable window, then blinks back to range. **Boomerang Cross-Slash** (Volley phase) answers pure kiting — a spinning blade crescent arcs out along a lane and returns. **Low-HP enrage** tightens all windup/recover/cooldown timers and burns the teal TrimRing brighter as a readable tell. Killing the Revenant wins the run; drop is the loot fountain from `BossBase` death spectacle. Mechanics: [actors/enemies/revenant_boss.gd](actors/enemies/revenant_boss.gd). |
| **9. Aspect drops** | ✅ Done (playtest tuning pending) | Full design: **[docs/BOON_DROPS.md](docs/BOON_DROPS.md)**. **M1 — Elite variants:** rare ×4-HP pool spawn past 4:00 (3%, ≤1 alive, 50s cooldown), ×1.3 scale, emissive glow, ×3 rewards, minimap ping + low SFX. **M2 — Aspect plumbing:** walk-over teal relic (`&"aspect"`) opening paused pick-1-of-2 `AspectScreen`; `BoonData` in `data/boons/aspects/registry.tres` (`unique = true`, grants flag, `requires_weapon`-gated); elite kills 1–2 per run drop relics, wave bosses (Revenant excluded) drop when all weapons are owned. **M3–M5 — Loadout Aspects:** Duelist (Crescendo riposte-chain, Mirror Ward projectile-reflect, Blade Waltz blink-slash), Earthshaker (Fault Line slam-refund + fissure, Epicenter leap wave-volley, Mass Driver shove-chain), Arcanist (Blood Pact HP-casting, Stormcaller levitate-as-flight with fork-3 bolts, Shatterflux chill-combos). **M6 — Universals:** Undying Will lethal-grace, Prospector's Idol gold+radius, Slipstream +charges/−cooldown; all headless-smoke-tested via `test/AspectSmoke.tscn`. Note: Stormcaller does not alter base Levitate; only when owned does Levitate become mana-sustained. |
| **10. Run recap & personal bests** | ✅ Done | Full design: **[docs/RUN_RECAP.md](docs/RUN_RECAP.md)**. The death/victory screen gained a post-run recap: "Slain by X" killer headline, boons/Aspects picked (rarity-tinted, in pick order), kills by enemy type with boss kill clocks, attributed damage taken per enemy, and persisted lifetime records (longest run, most kills, best level, most gold, victories, fastest victory) with NEW BEST badges. Plumbing: `RunStats` child of RunDirector + three new EventBus signals (`player_hit` — the full-attribution seam the telemetry plan also wants, `boon_picked`, `aspect_picked`); `MetaProgression.records` (additive save change, no migration). Headless harness: `test/RecapSmoke.tscn`. |
| **11. Depths — post-victory ascension** | ✅ Done (balance playtest pending) | Full design: **[docs/DEPTHS.md](docs/DEPTHS.md)**. Each victory opens the next Depth — a harder overlay on the same 7:30 run (goal ladder: 3 loadouts × 5 Depths). **M1 — core plumbing:** `DepthData`/`DepthRegistry` + 5 authored `.tres` (compounded supersets: I THE FLOOR BELOW ×1.3 HP … V THE REVENANT'S HOUR ×3.1 HP), `MetaProgression.selected_depth` (persisted, clamp-validated), `best_depth`/`depth_wins` records, Spawner/RunDirector fold the mults at the WaveTable call sites (null depth = Surface, byte-identical). **M2 — surfacing:** death-screen Depth picker (SURFACE · I · … deepest+1), first-win/depth-clear victory banners, `Deepest` records entry, HUD DEPTH chip, run-start announcement + low descend stinger. **M3 — identity twists & juice:** combined local event array fires the Twin Court's second Juggernaut (2:38, desynced charges); Depth V's Revenant at 6:45; `EnemyBase.depth_time_scale` windup compression (set/reset per run); earlier/wider elite windows; boss waves drop `boss_relic_count` Aspect relics (spawn-pause holds for the last pick); boon-rarity clock bonus + once-per-run pinned Legendary (IV+); duplicated-Environment ambient tint. **M4 — status lane:** loadout × Depth badge grid, `FASTEST — DEPTH N` NEW BEST entries, per-weapon `DepthTrim` emissive mesh tinted by that loadout's deepest clear (`DepthData.theme_color`, V echoes the Revenant teal), "DUELIST OF THE THIRD" title line. **M5 — shards & the Reliquary:** boss kills at Depth N bank N shards on the spot (saved immediately — attempts pay), clears add 2×N; `UpgradeData.currency`/`requires_depth`; RELIQUARY shard-priced branch (Second Thoughts, Deep Cache, Wider Fate, Fourth Card) + three forge capstones adding depth-themed Aspects to the relic pool (THE FLOOR BELOW kill-tremor, THE PRESSING DARK slam-slow, THE TWIN COURT riposte echo); `AspectPool` now honors `requires_any_ability`. Headless harness: `test/DepthSmoke.tscn`. **Forge wave 2 (2026-07-12, branch `forge-wave-2`):** the remaining 11 forged Aspects shipped (4 universal, 7 loadout-themed), bringing the Aspect registry to 26 and the forge tree to 14 capstones; new infra along the way — `AttackInfo.no_proc`, an `EnemyBase` ticking-stack DoT tracker, `FrostRune`, the Deep Draught overcharge input, and `EventBus.mana_absorbed`. **Left:** the M6 balance playtest (tune difficulty against the juice lane per the doc's risk notes). |
| **12+ (parked)** | — | Prestige layer, multiple arenas, achievements, gameplay telemetry ([docs/GAMEPLAY_TELEMETRY.md](docs/GAMEPLAY_TELEMETRY.md)). |

---

## 6. Risks / open questions

- **Melee feel in first person is the make-or-break.** Substantially addressed: the swing went through several iterations (bezier arc → shoulder-orbit rigid arm → three-phase windup/sweep/backswing, base swing slowed to 0.7s) and hits land with damage numbers, scale-pop, camera shake, and (Phase 4) synthesized SFX — whoosh on swing, thud on connect, distinct parry ping. Remaining levers if feel still falls short: hit-pause, and replacing the synth sounds with recorded assets in the art pass. Mechanics: [docs/COMBAT.md](docs/COMBAT.md).
- **Renderer:** project is on GL Compatibility — with GDScript that's the right default, since it's the only renderer that reaches web and low-end hardware, which is exactly the platform flexibility being prioritized. Revisit Forward+ in Phase 4 only if the visual target demands it *and* web is off the table by then.
- **GDScript horde performance:** holding. The alive cap ramps 15 → 60 (`WaveTable`), well under the ~200 danger zone, and pickups were deliberately built without physics bodies so hundreds of coins stay cheap. If counts grow past ~200, the escalation path in §1/§3.6 (typed scripts → manager-iterated steering → PhysicsServer3D) is the plan; profile before escalating.
- **Block design:** the "stamina cost if blocking proves too dominant" option got exercised (2026-07-03) — indefinite corner-turtling was dominant, so blocking is now a **guard meter**: 3s of hold time, −0.5s per blocked hit, perfect blocks free, and an empty meter breaks the block until it fully refills (~3s exposed). The parry window is untouched. Watch: if guard breaks feel unfair in swarms, raise `GUARD_HIT_COST`'s perfect-block exemption visibility (feedback), not the meter size, first.
- **Boon economy tuning (new):** reroll doubling (10 → 20 → 40 …) and skip payouts (15 + 5×level gold) are first-guess numbers; skip could become the dominant strategy for meta-focused players. Track gold-per-run from skips vs drops. Levers listed in [docs/BOONS.md](docs/BOONS.md).
- **Warhammer balance (Phase 5 pass):** nerfed to 20 dmg / 1.4s swing / 2.0m core + 3.6m splash, and the slam is now a **210° frontal arc** (blind wedge behind you) with only the inner core pushing. Spells are staff-exclusive now, so the old slam-AoE-plus-Frost-Nova trivialization is gone. Remaining watch: the CC uniques (Implosion gather-to-centre on the Seismic Slam, Riptide, Bone Breaker) at the 90 alive cap — dense clumps are also many windups on one spot; tune the gather/stagger before the shove if gather-then-slam becomes a suicide button.
- **Lethality & pacing tuning (first-pass numbers, calibrate by feel):** base HP is 100; XP is the geometric `14 × 1.14^level` curve (target ~10–30s/level); damage growth softened to +0.30/min and the opening slowed (`start_interval` 2.2). Swarms fire on a rhythmic beat (sprinters ~50s, chasers ~80s, a big spitter wave later). **Per-loadout trees mean a fresh hammer/staff run starts with no base upgrades** — intended (re-experience early scaling), but watch that it doesn't feel punishing. The ~200-enemy GDScript danger zone (§1) is still comfortably far; profile if swarms stack with the minute-5 Caster wave (dodging eruptions while a Sprinter swarm closes is the flagged difficulty-cliff risk — thin the 300 s-adjacent swarm timing before nerfing the boss).
- **Aspect drops tuning (new):** elite yield is the first dial — cap 2 spawns at 50s cooldown, 3% chance; if runs feel Aspect-flooded, cut the cap to 1 before touching boss drops. **Fault Line refund** (0.75s/enemy) is the scariest number — a wave through 8+ foes near-resets the slam; halve the refund before shrinking the fissure if slam-spam trivializes the third act. **Crescendo** turns swarms into riposte fuel; watch it against Stalker packs (whose whole counter is parry→riposte). **Blood Pact + Undying Will** in one run is a deliberate glass-cannon; confirm Undying's once-per-run doesn't chain. Elites must stay *optional* bounties, not gates — if playtest says "I have to kill it," soften HP before touching rate.

[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://core/upgrade_data.gd" id="1_script"]

[resource]
script = ExtResource("1_script")
id = &"forge_patient_dark"
display_name = "Forge: The Patient Dark"
description = "Add the sword Aspect THE PATIENT DARK to the relic drop pool — six seconds unhit primes your next swing as a full riposte. Forged, never auto-equipped."
base_cost = 16
max_level = 1
grants_ability = &"forge_patient_dark"
branch = &"reliquary"
currency = &"shards"
requires_depth = 2

[gd_resource type="Resource" script_class="UpgradeData" load_steps=2 format=3]

[ext_resource type="Script" path="res://core/upgrade_data.gd" id="1_script"]

[resource]
script = ExtResource("1_script")
id = &"forge_twin_court"
display_name = "Forge: The Twin Court"
description = "Add the sword Aspect THE TWIN COURT to the relic drop pool — every riposte is answered by a phantom twin. Forged, never auto-equipped."
base_cost = 20
max_level = 1
grants_ability = &"forge_twin_court"
branch = &"reliquary"
currency = &"shards"
requires_depth = 3
